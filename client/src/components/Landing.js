import React from 'react';

const Landing = () => {
    return (
      <div style={{ textAlign: 'center' }}>
          <h1>
              PGA Leaderboard
          </h1>
          PGA Leaderboard
      </div>
    );
};

export default Landing;